# This is a basic workflow to help you get started with Actions

name: Create OpenDataSet WebPage

# Controls when the workflow will run
on:
  workflow_dispatch: 
  # Triggers the workflow on push or pull request events but only for the master branch
  # push:
  #   branches: [ master ]

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:     
      - name: Fetch Repo Name
        id: repo-name
        run: echo "::set-output name=value::$(echo '${{ github.repository }}' | awk -F '/' '{print $2}')"
        
      - name: Pull OpenDataPage
        uses: actions/checkout@v2
        with:
          repository: 'whoohoo86/gh-open-data-page'
          ref: '77aca803b165116b9da49a4efe10b1250d5dfa20'
          # Stable desing commit ref
        
      - name: Create datasource.json
        uses: whoohoo86/gh-open-data-page-ds-action@cc76faa86561b3a7e3a10e65ddfb9675e5871ab9
        # Stable desing commit ref
        with:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          CWD:  ${{ github.workspace }}

      # Runs a single command using the runners shell
      - name: Setup Node.js environment
        uses: actions/setup-node@v2.5.1
        with:
          node-version: 14.x

      # Runs a set of commands using the runners shell
      - name: Prepare and deploy
        env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: |
          cd $GITHUB_WORKSPACE
          npm install
          npm run ng -- deploy --repo=https://github.com/$GITHUB_REPOSITORY.git --base-href=/${{ steps.repo-name.outputs.value }}/ --name=RKIOpenData --email=OpenData@rki.de --branch=open-data-website